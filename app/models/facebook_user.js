const user = require(global.include.model.user);
const db = require(global.include.db);

var facebook_user = function(sql_result){

}

exports.get_facebook_user_by_id = function(id){
	db.get().query('INSERT INTO ')
}
